console.error(
  [
    "This Node server is not used by the Guess the Number game.",
    "It is a leftover BMI example server.",
    "",
    "Run the Python backend instead:",
    "  python app.py",
    "",
    "Then open:",
    "  http://127.0.0.1:8010"
  ].join("\n")
);

process.exit(1);
